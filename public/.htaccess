<IfModule mod_rewrite.c>
    RewriteEngine On

    # Let Apache serve real files such as CSS, JS, images, and fonts.
    RewriteCond %{REQUEST_FILENAME} -f
    RewriteRule ^ - [L]

    # Route everything else through Symfony.
    RewriteRule ^ index.php [QSA,L]
</IfModule>

